import{NextRequest,NextResponse}from"next/server";import{isAdmin}from"@/lib/admin-auth";import{query}from"@/lib/db";
async function ensure(){await query(`CREATE TABLE IF NOT EXISTS inquiries(id BIGSERIAL PRIMARY KEY,name TEXT NOT NULL,email TEXT NOT NULL,phone TEXT,reason TEXT,message TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'new',created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`)}
export async function GET(){if(!(await isAdmin()))return NextResponse.json({message:'Unauthorized'},{status:401});await ensure();const r=await query(`SELECT * FROM inquiries ORDER BY created_at DESC`);return NextResponse.json({inquiries:r.rows})}
export async function PATCH(req:NextRequest){if(!(await isAdmin()))return NextResponse.json({message:'Unauthorized'},{status:401});const b=await req.json();if(!['new','contacted','closed'].includes(b.status))return NextResponse.json({message:'Invalid status.'},{status:400});await query(`UPDATE inquiries SET status=$1 WHERE id=$2`,[b.status,b.id]);return NextResponse.json({ok:true})}
