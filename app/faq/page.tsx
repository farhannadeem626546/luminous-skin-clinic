import PageHero from "@/components/PageHero";import{faqs as defaults}from"@/data/site";import{getFaqs}from"@/lib/cms";export const dynamic="force-dynamic";export const metadata={title:"FAQ"};export default async function FAQ(){const db=await getFaqs();const items=db.length?db.map(x=>[String(x.question),String(x.answer)]):defaults;return <><PageHero eyebrow="Questions answered" title="Everything you need before booking." text="Clear answers reduce uncertainty and help clients arrive prepared."/><section className="section"><div className="container narrow-wide faq-list">{items.map(([q,a])=><details key={q}><summary>{q}<span>+</span></summary><p>{a}</p></details>)}</div></section></>}
