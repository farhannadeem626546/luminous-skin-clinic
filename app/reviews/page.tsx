import PageHero from "@/components/PageHero";import{reviews as defaults}from"@/data/site";import{getReviews}from"@/lib/cms";export const dynamic="force-dynamic";export const metadata={title:"Reviews"};export default async function Reviews(){const db=await getReviews();const items=db.length?db.map(r=>({name:String(r.client_name),treatment:String(r.treatment_name||"Client"),quote:String(r.review_text),rating:Number(r.rating||5)})):defaults.map(r=>({...r,rating:5}));return <><PageHero eyebrow="Client stories" title="Trust is built through the experience." text="Verified customer feedback from the Luminous client journey."/><section className="section dark"><div className="container review-grid">{items.map(r=><blockquote key={r.name+r.quote}><div className="stars">{"★".repeat(r.rating)}</div><p>“{r.quote}”</p><footer><strong>{r.name}</strong><span>{r.treatment}</span></footer></blockquote>)}</div></section></>}
