"use client";import{useEffect,useState}from"react";type Item={id:number;name:string;email:string;phone?:string;reason:string;message:string;status:string;created_at:string};
export default function InquiryDashboard(){const[items,setItems]=useState<Item[]>([]);const[msg,setMsg]=useState('');async function load(){const r=await fetch('/api/admin/inquiries');const d=await r.json();setItems(d.inquiries||[])}useEffect(()=>{load()},[]);async function status(id:number,value:string){const r=await fetch('/api/admin/inquiries',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,status:value})});setMsg(r.ok?'Enquiry updated.':'Update failed.');if(r.ok)load()}return <section className="admin-content"><div className="admin-heading"><div><span className="eyebrow">Contact submissions</span><h1>Enquiries</h1></div><a className="button secondary" href="/admin">Back to dashboard</a></div>{msg&&<p className="admin-notice">{msg}</p>}<div className="admin-table-wrap"><table><thead><tr><th>Client</th><th>Reason</th><th>Message</th><th>Received</th><th>Status</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><b>{x.name}</b><small><a href={`mailto:${x.email}`}>{x.email}</a><br/>{x.phone||''}</small></td><td>{x.reason}</td><td>{x.message}</td><td>{new Date(x.created_at).toLocaleString('en-GB')}</td><td><select value={x.status} onChange={e=>status(x.id,e.target.value)}><option value="new">New</option><option value="contacted">Contacted</option><option value="closed">Closed</option></select></td></tr>)}</tbody></table></div>{!items.length&&<div className="empty-state"><h2>No enquiries yet.</h2><p>New contact-form submissions will appear here.</p></div>}</section>}
