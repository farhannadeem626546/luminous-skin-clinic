"use client";import{FormEvent,useState}from"react";
export default function ContactForm(){const[f,setF]=useState({name:'',email:'',phone:'',reason:'Treatment question',message:''});const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMsg('');const r=await fetch('/api/inquiries',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const d=await r.json();setMsg(d.message||'Unable to send enquiry.');if(r.ok)setF({name:'',email:'',phone:'',reason:'Treatment question',message:''});setBusy(false)}return <form className="contact-form" onSubmit={submit}><label>Name<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Email<input required type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Phone<input type="tel" value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></label><label>Reason<select value={f.reason} onChange={e=>setF({...f,reason:e.target.value})}><option>Treatment question</option><option>Booking help</option><option>Aftercare</option><option>Other</option></select></label><label className="full-span">Message<textarea required rows={6} value={f.message} onChange={e=>setF({...f,message:e.target.value})}/></label><button disabled={busy} className="button primary">{busy?'Sending...':'Send enquiry'}</button>{msg&&<p className="full-span admin-notice" role="status">{msg}</p>}</form>}
